# Anthropic — LLM calls for decomposition + synthesis
ANTHROPIC_API_KEY=

# Coinbase Developer Platform — Server Wallet v2
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=

# Base mainnet RPC (optional — CDP handles signing remotely)
BASE_RPC_URL=https://mainnet.base.org

# Mock mode — set to "true" to skip real x402 calls and use canned responses
MOCK_MODE=true

# Server configuration
PORT=3000
AGENT_URL=https://your-app.railway.app
FACILITATOR_URL=https://facilitator.payai.network

# Pinata — IPFS pinning for ERC-8004 content-addressed metadata
# Get a free JWT at https://app.pinata.cloud/developers/api-keys
PINATA_JWT=
